# conditions can be:
# ==, !=, <=, >=, <, and >
# A semicolon is used to define the end of the condition.

num a = 1
num b = 1

if (a == b)
	println("a is equal to b")
;

# This condition won't run:
if (a != b)
	println("a is not equal to b")
;
